ALLOW_CLIENT_S3_CREDENTIALS: true
AWS_BUCKET: fooBar # this can be overridden on our dev branch, don't expect it in build, rely on injected value

datasafe:
  systemRoot: file:///tmp/
  amazonAccessKeyID: access
  amazonSecretAccessKey: secret
  amazonRegion: eu-central-1
